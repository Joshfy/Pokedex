export interface PokemonColors {
  colorPalette: {
    primary: string;
    secondary: string;
    accent: string;
    background: string;
    text: string;
    highlights: string[];
    locked: boolean;
  };
  shinyColorPalette: {
    primary: string;
    secondary: string;
    accent: string;
    highlights: string[];
  };
}
